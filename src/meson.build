sources = ['webcam_whitebalance.vala']

shared_library('webcam_whitebalance',
  sources,
  dependencies: [budgie_dep, gtk_dep, glib_dep],
  install: true,
  install_dir: get_option('libdir') / 'budgie-desktop/plugins'
)